[pytest]
testpaths = tests
addopts = -q --strict-markers
markers =
    ollama: braucht ein laufendes Ollama auf 127.0.0.1:11434 (nur mit --ollama)
    slow: lädt ML-Modelle oder dekodiert Audio (nur mit --slow)
